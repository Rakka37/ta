# neuro_fuzzy.py

# Impor pustaka yang diperlukan
import pandas as pd
import numpy as np

# Fungsi placeholder karena pustaka `anfis` tidak tersedia
def neuro_fuzzy_recommendation(book_id, rating):
    # Placeholder sederhana yang mengembalikan rata-rata dari input
    return (book_id + rating) / 2
